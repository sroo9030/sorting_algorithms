#include "sort.h"

/**
  * bubble_sort - a function that sorts an array of integers
  * @array: a pointer to an array to sort
  * @size: the size
  */
void bubble_sort(int *array, size_t size)
{
	size_t i, j;
	int temp, *sort;

	for (i = 0; i <  size - 1; i++)
	{
		for (j = 0; j < size - 2; j++)
		{
			if (array[i]  > array[i + 1])
			{

			}
		}
	}

}
